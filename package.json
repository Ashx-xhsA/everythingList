{
  "name": "autofocus-monorepo",
  "private": true,
  "scripts": {
    "dev:web": "npm run dev -w web",
    "build:web": "npm run build -w shared && npm run build -w web"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
